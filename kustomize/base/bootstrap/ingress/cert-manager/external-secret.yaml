apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: cloudflare
spec:
  secretStoreRef:
    name: aws-param
    kind: ClusterSecretStore
  data:
  - secretKey: cloudflare
    remoteRef:
      key: /home/cloudflare/cloudflare_api_token